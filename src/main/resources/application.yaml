spring:
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.mongo.MongoAutoConfiguration

mongodb:
  primary:
    host: localhost
    port: 27017
    database: user_db
  secondary:
    host: localhost
    port: 27017
    database: student_db